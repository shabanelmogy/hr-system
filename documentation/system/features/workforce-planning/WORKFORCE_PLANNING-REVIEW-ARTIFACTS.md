# Workforce Planning Review Artifacts

This is the Phase 0 evidence ledger for the new `workforce-planning` feature.
It separates verified repository behavior from the frozen requested contract.

## Metadata

| Field | Value |
| --- | --- |
| Feature | `workforce-planning` |
| API route | `/api/v1/workforce-planning/plans` |
| Web route | `/workforce-planning/plans` |
| Mobile route | `/(main)/workforce-planning` |
| Review owner | Engineering and Product Architecture |
| Review date | `2026-09-06` |
| Operating mode | New feature |
| Applied reference | `states` |
| Documentation state | Phase 1 Workforce Plans corrected across API, Web, and Mobile; database gate awaits a user-created migration; final only after the full chain and phase 06 |
| Implementation request | `documentation/system/features/workforce-planning/IMPLEMENTATION-REQUEST.md` |
| Required-file manifest | `documentation/system/features/workforce-planning/required-files.draft.json` |

## Verified current repository evidence

| Evidence ID | Verified behavior | Source and symbol | Phase 0 consequence |
| --- | --- | --- | --- |
| E-01 | FiscalYear is the company-scoped financial calendar; Draft is editable and lifecycle is Draft -> Open -> Closing -> Closed -> Locked. | `api/HrManagementSystem.Domain/Finance/FiscalYears/Entities/FiscalYear.cs: FiscalYear`, `FiscalYearStatus.cs` | Workforce Planning consumes FiscalYear/FiscalPeriod identities and never creates a second calendar. |
| E-02 | Fiscal periods are generated by domain code for Monthly or Quarterly frequency and cannot be authored independently. | `FiscalYear.RebuildPeriods`, `FiscalPeriod.cs` | Plan period targets use server-owned FiscalPeriod IDs and reconcile by sequence. |
| E-03 | Fiscal Years migration exists and EF reports no pending model changes. | `api/HrManagementSystem.Infrastructure/Migrations/20260905180523_AddFiscalYears.cs`; EF migration check | The foundation is available for the next additive migration. |
| E-04 | JobOffer currently supports only statuses 1 through 6 and `Issue` accepts Draft directly. | `JobOfferStatus.cs`, `JobOffer.Issue` | Phase 5 appends 7 and 8, adds approval history, and gates issuance. |
| E-05 | Active-offer uniqueness currently filters statuses 1, 2, and 3. | `JobOfferConfiguration.cs` | Phase 5 includes PendingApproval and Approved in the filter. |
| E-06 | JobRequisition currently has `IsBudgeted` and an unbudgeted justification path. | `JobRequisition.cs`, `JobRequisitionConfiguration.cs` | Planned V1 requisitions use StaffingRequest; legacy rows remain readable. |
| E-07 | Recruitment controllers depend on `IRecruitmentService`; the current service is a broad legacy boundary. | `api/HrManagementSystem.Api/Features/Recruitment/V1/*Controller.cs`, `RecruitmentService.cs` | Workforce Planning does not introduce another aggregate service. |
| E-08 | Current HireApplicationAsync performs multiple SaveChangesAsync calls and uses DateTime.UtcNow. | `RecruitmentService.HireApplicationAsync` | Phase 5 replaces this slice with one idempotent atomic command and TimeProvider. |
| E-09 | ApplicationDbContext exposes deterministic SQL Server transaction locks through ExecuteAtomicallyAsync. | `ApplicationDbContext.ExecuteAtomicallyAsync`, `IUnitOfWork` | Atomic hire and capacity reservations use this existing port. |
| E-10 | Position stores a company-scoped TargetHeadcount and DivisionId. | `api/HrManagementSystem.Domain/OrganizationalStructure/Entities/Position.cs` | Plan baseline and Envelope authorization are separate snapshots; Position.TargetHeadcount is not silently synchronized. |
| E-11 | Workforce Plans now provide the full Draft -> Submitted -> UnderReview -> Approved/Rejected lifecycle, row-version protected actions, revision history, an automatic branch/date-aware employee baseline, separate new-position and replacement demand, derived target/total figures, server realtime dispatch, shared Grid/Cards and governed forms on Web, equivalent table/cards/forms/actions on Mobile, runtime response schemas, and focused domain/persistence tests. | `api/.../WorkforcePlanning/*`, `web-next/src/features/workforce-planning/*`, `mobile-react/src/features/workforce-planning/*`, `WorkforcePlanDomainTests.cs`, `WorkforcePlanPersistenceTests.cs` | Phase 1 source is ready for its database gate; do not begin budget/envelope runtime until the corrective migration is created and applied by the user. |
| E-12 | Live create diagnostics found legacy required columns (`EstimatedSalary`, `EstimatedRecruitmentCost`, `Q1Target`-`Q4Target`) and nullability drift on the pre-existing `WorkforcePlanLines` table. The current model snapshot cannot auto-detect these database-only leftovers. | SQL Server `INFORMATION_SCHEMA.COLUMNS`; create trace `0HNOBVED2P27D:00000001` | A user-created manual corrective migration is required before Phase 1 database acceptance. |

## Frozen requirement matrix

| ID | Requirement | API | Web | Mobile | Decision | Phase |
| --- | --- | --- | --- | --- | --- | --- |
| R-01 | FiscalYear-to-Employee trace | Required | Required | Required | One bounded trace chain; no second calendar | 1-6 |
| R-02 | Plan revisions and period hiring targets | Required | Required | Required | Plan owns demand only; no authoritative money | 1 |
| R-03 | Budget allocation and currency parity | Required | Required | Required | Salary and recruitment categories are explicit | 2 |
| R-04 | Automatic Position Envelopes | Required | Required | Required | Generated only at budget activation | 2 |
| R-05 | Positive Envelope Amendments | Required | Required | Required | Submitted/Approved lifecycle; anti-self approval | 3 |
| R-06 | Dual headcount/fiscal-cost Staffing Request | Required | Required | Required | Atomic reservation and release math | 3 |
| R-07 | Planned requisition bridge | Required | Required | Required | Approved StaffingRequest is mandatory for new planned rows | 4 |
| R-08 | Legacy compatibility | Required | Read-only visibility | Read-only visibility | Server-only Legacy classification; no fabricated history | 4 |
| R-09 | Job Offer governance | Required | Required | Required | Append statuses 7 and 8; preserve 1-6 | 5 |
| R-10 | Atomic idempotent hire | Required | Required | Required | One transaction and one save for hire | 5 |
| R-11 | Journey trace | Required | Required | Required | Web motion tree plus accessible list; mobile timeline | 6 |
| R-12 | Plan vs Commitment | Required | Required | Required | Server projection; no payroll actuals in V1 | 6 |

## Platform capability decisions

| Capability | API | Web | Mobile | Evidence/decision |
| --- | --- | --- | --- | --- |
| Grid/Table | Required | Required | Required | Shared server-list and responsive list primitives |
| Cards | Required | Required | Required | Capacity and approval summaries |
| Create/Edit/View forms | Required | Required | Required | MyForm/Zod and AppForm/repeater builders |
| Revision comparison | Required | Required | Required | Plan governance with responsive detail on mobile |
| Trace visualizer | Required | Required | Required | Framer Motion tree on Web; chronological timeline on Mobile |
| Plan vs Commitment report | Required | Required | Required | Projected DTO; Crystal/Payroll integration Deferred |
| Chart | Excluded | Excluded | Excluded | V1 keeps numeric/table variance only |
| Import/Export | Deferred | Deferred | Deferred | Manual governed entry is sufficient for V1 |

## Lifecycle and invariant tables

| Aggregate | Allowed transitions | Non-negotiable invariant |
| --- | --- | --- |
| FiscalYear | Existing lifecycle only | Workforce approvals and reservations require Open |
| WorkforcePlan | Draft -> Submitted -> UnderReview -> Approved/Rejected; Approved -> Superseded | One effective revision; period slot sum equals PlannedHiringSlots |
| WorkforceBudget | Draft -> Submitted -> Approved/Rejected; Approved -> Superseded/Closed | AuthorizedHeadcount <= PlannedHiringSlots; category allocations reconcile |
| PositionEnvelope | System-generated active snapshot | Available headcount and salary never fall below zero |
| EnvelopeAmendment | Draft -> Submitted -> Approved/Rejected | Positive deltas only; requester cannot approve |
| StaffingRequest | Draft -> Submitted -> Approved/Rejected/Cancelled/Fulfilled | Cancellation releases only the unallocated/unhired remainder |
| Planned JobRequisition | Draft -> Submitted -> Approved/Rejected/Cancelled/Fulfilled | Approved StaffingRequest and remaining quota are required |
| JobOffer | Draft -> PendingApproval -> Approved -> Issued -> Accepted/Declined/Withdrawn/Expired | Issuance requires Approved; statuses 1-6 remain unchanged |
| Hire | Idempotent command | Accepted offer, capacity transition, and Employee graph commit atomically |

## Financial policy evidence

`CalculationPolicyVersion=2026-09-V1` is frozen in the implementation request.
The authoritative commitment is the rounded fiscal-year pro-rata cost, not an
annualized display value. Every reservation stores currency, policy version,
annualized salary, fiscal-year cost, and source start date. Salary and recruitment
costs reconcile separately; a missing category allocation is a validation error.

## Database and rollout evidence

The current EF migration chain ends at the user-created
`20260906095027_RepairWorkforcePlanLinesLegacySchema`. The
read-only checks run during Phase 0 were:

```text
./documentation/system/Generate-Documentation.ps1 -Check
dotnet ef migrations list --project api/HrManagementSystem.Infrastructure/HrManagementSystem.Infrastructure.csproj --startup-project api/HrManagementSystem.Api/HrManagementSystem.Api.csproj --no-build
dotnet ef migrations has-pending-model-changes --project api/HrManagementSystem.Infrastructure/HrManagementSystem.Infrastructure.csproj --startup-project api/HrManagementSystem.Api/HrManagementSystem.Api.csproj --no-build
```

Results at the initial Phase 1 review: documentation check passed for 63 registered
recipes and `AddWorkforcePlanningPlans` was already applied. The corrective review
found legacy non-null columns in the existing empty `WorkforcePlanLines` table and
a non-unique effective-plan index. Source now models the correct invariant, so EF
required a user-created manual migration because the legacy columns were absent
from the EF snapshot. `RepairWorkforcePlanLinesLegacySchema` now contains guarded
SQL and remains pending application. No migration was created or applied by Codex,
per the user's migration-ownership instruction.

The user-created `modifyPahse01` migration replaces
`IX_WorkforcePlans_TenantId_CompanyId_FiscalYearId_Status_ActivatedOn_SupersededOn`
with the unique filtered `UX_WorkforcePlans_OneEffectivePerFiscalYear` index. The
later `RepairWorkforcePlanLinesLegacySchema` migration uses guarded SQL to drop
`EstimatedSalary`, `EstimatedRecruitmentCost`, `Q1Target`, `Q2Target`, `Q3Target`,
and `Q4Target` from `WorkforcePlanLines`, plus `SubmittedByEmployeeId`,
`ApprovedByEmployeeId`, and `ReviewNotes` from `WorkforcePlans`. It also aligns
`BranchId` as nullable, `DivisionId` as required, and removes the legacy default
constraint from `NewHireSlots`. Both affected tables were reverified at zero rows
before authoring; the migration aborts if legacy data appears before application.

Planned additive migrations are `AddWorkforcePlanningPlans`,
`AddWorkforceBudgetsAndPositionEnvelopes`, `AddStaffingRequestsAndEnvelopeAmendments`,
`LinkStaffingRequestsToJobRequisitions`, and `AddJobOfferApprovalGovernance`.

## Phase 0 gate

| Gate item | Result | Evidence |
| --- | --- | --- |
| Documentation baseline check | Pass | `Generate-Documentation.ps1 -Check` passed |
| Reference selection | Pass | States profiles and Fiscal Years review read and recorded |
| Current source inventory | Pass | Evidence E-01 through E-11 |
| Contract contradictions removed | Pass | No Exceptional PlanningSource; plan has no authoritative money; activation/amendment lifecycle frozen |
| Financial calculation policy | Pass | `CalculationPolicyVersion=2026-09-V1` and separate category reconciliation |
| Platform decisions | Pass | Required/Deferred/Excluded matrix above |
| Runtime changes during Phase 0 | Pass | No runtime change was made until Gate 0; Phase 1 changes are recorded below |
| User approval | Received | User requested step-by-step implementation after plan review |

Gate 0 remains complete. Phase 1 domain, API, realtime, Web, Mobile, localization,
and focused-test source is implemented and corrected. Its database gate is Pending:
the user must create, review, and apply `FixWorkforcePlanningPhase1Integrity`, then
run the pending-model check and a live create/edit/lifecycle smoke test. The draft
manifest remains unregistered until that gate and the later full-chain evidence
surface are complete. Phase 2 budget/envelope runtime must not start before this
database gate passes.
