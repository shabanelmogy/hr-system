import {
  parseRealtimeEntityChanged as parseEntityChanged,
  parseRealtimeNotification as parseNotification,
} from '../data/remote/realtime-event-parser';

export const parseRealtimeEntityChanged = parseEntityChanged;
export const parseRealtimeNotification = parseNotification;
